import {
	add, remove, edit, count, isEmpty, editAll, removeAll, get,
} from '../lib/store';

const TodoManager = {
	get,
	add,
	edit,
	remove,
	count,
	isEmpty,
	editAll,
	removeAll,
};

export default TodoManager;
